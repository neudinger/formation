#pragma once

#include <vector>

namespace course {

std::vector<float> vector_add_cpu(const std::vector<float> &a,
                                  const std::vector<float> &b, float alpha);

} // namespace course
